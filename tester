import websocket, json

app_id = "1089"  # or your own registered app_id
token = "pat_9e9d988710e9b09cac39396028f5c999bdd6569b73d3e94f3c3ce1e672baa79f".strip()

ws = websocket.create_connection(f"wss://ws.derivws.com/websockets/v3?app_id={app_id}")
ws.send(json.dumps({"authorize": token}))
print(ws.recv())
ws.close()